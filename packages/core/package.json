{
  "name": "@yrdsl/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types, validation, and schemas.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./validation": "./src/validation/index.ts",
    "./schemas": "./src/schemas/index.ts"
  },
  "scripts": {
    "build": "echo 'core is consumed as source' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
