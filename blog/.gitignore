# Generated by _build.mjs from _src/posts/. Source is _src/, output is
# everything else (index.html, <slug>/, tags/, feed.xml). Build runs
# during deploy-landing.yml; only _src/ + _build*.mjs + .gitignore
# belong in git.
*
!_src/
!_src/**
!_build.mjs
!_build.test.mjs
!.gitignore
