{
  "name": "@yrdsl/landing",
  "version": "0.0.0",
  "private": true,
  "description": "Marketing landing page for yrdsl.app (GH Pages).",
  "scripts": {
    "build": "node blog/_build.mjs",
    "dev": "node blog/_build.mjs && npx -y serve@latest -l 4173 .",
    "typecheck": "echo 'no ts' && exit 0",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo blog/index.html blog/feed.xml blog/tags blog/welcome blog/*-*-*"
  },
  "devDependencies": {
    "js-yaml": "^4.1.1",
    "marked": "^18.0.2",
    "vitest": "^2.1.9"
  }
}
